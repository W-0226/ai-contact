[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: 标记耗时较长的测试
    local: 本地规则引擎相关测试
    llm: LLM引擎相关测试（需要API key）
    reminder: 提醒生成器相关测试
    integration: 集成测试
